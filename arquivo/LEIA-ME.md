# arquivo/

Material antigo mantido por segurança — **nada aqui é usado pelo site**
(o build do Vite só publica `index.html` da raiz + `public/`).

- `SIte juris 8/` — export antigo do design system (HTML + assets + uploads).
- `teste.html` — redirect de teste para uma `landing-page.html` que não existe mais.
- `logo-antiga-quadrada.png` — antiga `logo.png` da raiz (611×408). Substituída
  em todas as páginas pelo wordmark `public/DET/logo.png` (513×145).

Pode ser consultado ou restaurado a qualquer momento; o histórico completo
também está no git.
